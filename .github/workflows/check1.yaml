name: Check
on: push
jobs: 
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install deps
        run: npm ci
      - name: lint
        run: npm run lint
      - name: Set Up Flutter
        uses: subosito/flutter-action@v2
        
        
